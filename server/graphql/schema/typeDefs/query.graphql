
type Query {

  # retuns the category for the ID
  category(id: ID!): Category

  # returns a list of all products (optionally filtered by category)
  categories: [Category]

  # returns a list of all products (optionally filtered by category)
  products(categories: [String]): [Product]

  # returns the products for the ID
  product(id: ID!): Product
}
