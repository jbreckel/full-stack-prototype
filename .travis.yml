
language: node_js
node_js:
  - node

cache:
  - node_modules

env:
  global:

before_install:
  - curl -o- -L https://yarnpkg.com/install.sh | bash
  - export PATH=$HOME/.yarn/bin:$PATH

install:
  - yarn

script:
  - yarn run test
  - yarn run build


branches:
  only:
  - master
